<%= form_for(@order_user) do |f| %>
  <% if @order_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_user.errors.count, "error") %> prohibited this order_user from being saved:</h2>

      <ul>
      <% @order_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :orderID %><br>
    <%= f.number_field :orderID %>
  </div>
  <div class="field">
    <%= f.label :listOfItems %><br>
    <%= f.text_area :listOfItems %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
